# Used by: All scripts for login
PORTAL_URL=https://app.nimar.gov.pk/
USERNAME=rabbia
PASSWORD=Forbmax@23

# --- Email (Gmail IMAP) [ALL] ---
# Used by: All scripts for OTP retrieval
EMAIL_USER=rabia.gillani@forbmax.ai
EMAIL_PASS=xznv ikxs ggmi saax
# IMAP server address
EMAIL_SERVER=imap.gmail.com

# --- Browser Settings [ALL] ---
# Used by: All scripts for browser configuration
BROWSER_HEADLESS=False
BROWSER_IGNORE_HTTPS_ERRORS=True
BROWSER_NO_VIEWPORT=True

# --- OTP Login Timings [OTP] ---
# Used by: auth/otp.py
# Note: Milliseconds for Playwright, seconds for time.sleep
OTP_CREDENTIAL_ENTRY_WAIT=4000
OTP_BUTTON_TIMEOUT=10000
OTP_EMAIL_WAIT_TIME=10000
OTP_INPUT_DELAY=200
OTP_VERIFY_BUTTON_TIMEOUT=8000
OTP_LOGIN_COMPLETE_WAIT=3000
OTP_RETRIES=15
OTP_DELAY=5

# --- Upload Workflow Timings [UPLOAD, VALIDATION] ---
# Used by: uploads/single-zipfile-upload.py, uploads/upload-sequence-validation.py
WAIT_TIMEOUT=20
UPLOAD_WAIT_TIME=20
STEP_GAP_SECONDS=2
LOGIN_SUCCESS_WAIT=5
CIRCLES_CLICK_WAIT=3
QA_CIRCLE_OPEN_WAIT=3
UPLOAD_BUTTON_SCROLL_WAIT=1
UPLOAD_CANCELED_DETECTION_TIMEOUT=3000
BROWSER_DIALOG_TIMEOUT=5.0
PORTAL_CONFIRM_WAIT=4000
START_UPLOAD_ENABLED_CHECK_INTERVAL=0.3
START_UPLOAD_SCROLL_WAIT=0.5
START_UPLOAD_CLICK_WAIT=3
ADD_METADATA_SCROLL_WAIT=0.5
ADD_METADATA_CLICK_WAIT=2
SUBMIT_FORM_WAIT=6
SUBMIT_AFTER_WAIT=10
MODAL_THUMBNAIL_SCROLL_WAIT=1
MODAL_OPEN_WAIT=10
DOWNLOAD_BUTTON_SCROLL_WAIT=0.3

# --- Retry Settings [UPLOAD, VALIDATION] ---
# Used by: uploads/single-zipfile-upload.py, uploads/upload-sequence-validation.py
WAIT_AND_CLICK_START_MAX_RETRIES=2
CLICK_PORTAL_START_UPLOAD_MAX_RETRIES=3
START_UPLOAD_ENABLED_CHECK_MAX_ATTEMPTS=10

# --- File Upload Paths [UPLOAD] ---
# Used by: uploads/single-zipfile-upload.py
# Desktop folder path (relative to USERPROFILE\Desktop)
DESKTOP_FOLDER=New folder
# ZIP file name to upload
ZIP_FILE=Ary news.zip
# Desktop base path (usually Desktop, but can be customized)
DESKTOP_PATH=Desktop

# --- Circle Name [UPLOAD, VALIDATION] ---
# Used by: uploads/single-zipfile-upload.py, uploads/upload-sequence-validation.py
CIRCLE_NAME=QA Testing

# --- Metadata Form Fields [UPLOAD, VALIDATION] ---
# Used by: uploads/single-zipfile-upload.py, uploads/upload-sequence-validation.py
POST_TITLE=Automated QA Upload Validation
CONTENT_TITLE=Single ZIP Upload Test
DESCRIPTION=This post was auto-generated during QA automation to validate upload workflow.
KEYWORDS=automation, qa, upload, zip-test

# --- Validation Script Settings [VALIDATION] ---
# Used by: uploads/upload-sequence-validation.py
# File names for validation (files will be loaded from Desktop\{DESKTOP_FOLDER} dynamically)
# Script automatically detects user profile and uses DESKTOP_FOLDER variable
# Just provide file names here, script will find them in Desktop\New folder
FILE_URL_1=ss.pdf
FILE_URL_2=Ary news.mp4
FILE_URL_3=image.png
# S3 Bucket URL (fallback if dynamic extraction fails)
S3_BUCKET_URL=https://minioapi.forbmax.ai/

# --- Logging [ALL] ---
# Used by: All scripts
LOG_LEVEL=INFO

# --- Live Stream Settings [LIVE] ---
# Used by: live/live-test-save-clip.py
LIVE_USE_SYSTEM_CHROME=True
LIVE_BROWSER_HEADLESS=False
LIVE_USE_CHROME_CHANNEL=False
WAIT_AFTER_GET_STREAM=5

# --- Elastic Search & Advanced Search Settings [ELASTIC_SEARCH] ---
# Used by: Elastic-search&-advance-search/elastic-search-advance-search-timeline.py
# Fuzzy matching threshold for keyword search (0-100, higher = stricter matching)
ELASTIC_SEARCH_FUZZY_THRESHOLD=70
# Noise words to filter out during search (comma-separated)
ELASTIC_SEARCH_NOISE_WORDS=other,see details,mb,mp4,jpg,png,zip,pdf,xls,mov
# Page load timeout in milliseconds
ELASTIC_SEARCH_PAGE_LOAD_TIMEOUT=20000
# Element wait timeout in milliseconds
ELASTIC_SEARCH_ELEMENT_WAIT_TIMEOUT=10000
# Scroll pause time in milliseconds
ELASTIC_SEARCH_SCROLL_PAUSE_TIME=500
# Default search keyword
ELASTIC_SEARCH_DEFAULT_KEYWORD=news
